<UserControl x:Class="Keycipher.ManagementApp.Views.Licenses.EditSingleValueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="300">
    <GroupBox Margin="5" Header="Modified Data">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="20" />
                <RowDefinition Height="40" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Left" Text="{Binding InputName}" />
            <TextBox Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" Width="150"
                     Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>
    </GroupBox>

</UserControl>